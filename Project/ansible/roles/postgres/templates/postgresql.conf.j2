listen_addresses = '*'
wal_level = replica
max_wal_senders = 5
wal_keep_size = 64
hot_standby = on

{% if postgres_role == 'replica' %}
primary_conninfo = 'host={{ postgres_primary_host }} user={{ postgres_replication_user }} password={{ postgres_replication_password }}'
{% endif %}
